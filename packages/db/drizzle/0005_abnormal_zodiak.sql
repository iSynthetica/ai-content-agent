-- НАВМИСНО ПОРОЖНЯ (no-op).
--
-- Ця міграція була згенерована drizzle-kit автоматично і дублювала 0004_run_progress
-- (`ALTER TABLE generation_runs ADD COLUMN progress jsonb`): 0004 писалась вручну, снапшот
-- drizzle про неї не знав, тож наступний `generate` вирішив, що колонки досі бракує.
--
-- Застосувати її неможливо — колонка вже існує (`column "progress" already exists`), і вона
-- блокувала будь-яку наступну міграцію. Файл лишаємо на місці (видалення зсунуло б індекси
-- журналу), але робимо безпечним no-op.
SELECT 1;
